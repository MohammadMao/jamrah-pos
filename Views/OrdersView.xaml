<UserControl x:Class="JamrahPOS.Views.OrdersView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:viewmodels="clr-namespace:JamrahPOS.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="1200">
    
    <UserControl.DataContext>
        <viewmodels:OrdersViewModel />
    </UserControl.DataContext>

    <Grid Background="#F5F5F5">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Filters Section -->
        <Border Grid.Row="0" Background="White" Padding="20" Margin="10,10,10,5">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Title and Refresh -->
                <Grid Grid.Row="0" Margin="0,0,0,15">
                    <TextBlock Text="إدارة الطلبات" 
                               FontSize="24" 
                               FontWeight="Bold" 
                               HorizontalAlignment="Right"
                               FlowDirection="RightToLeft"/>
                    <Button Content="🔄 تحديث" 
                            Command="{Binding RefreshCommand}"
                            FontSize="14"
                            Padding="15,8"
                            Background="#2196F3"
                            Foreground="White"
                            BorderThickness="0"
                            Cursor="Hand"
                            HorizontalAlignment="Left"
                            FlowDirection="LeftToRight"/>
                </Grid>

                <!-- Filters -->
                <Grid Grid.Row="1" FlowDirection="RightToLeft">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <!-- Date Range -->
                    <StackPanel Grid.Column="0" Margin="0,0,15,0">
                        <TextBlock Text="من تاريخ" FontSize="12" Margin="0,0,0,5"/>
                        <DatePicker SelectedDate="{Binding StartDate}" 
                                    FontSize="14" 
                                    Width="140"/>
                    </StackPanel>

                    <StackPanel Grid.Column="1" Margin="0,0,15,0">
                        <TextBlock Text="إلى تاريخ" FontSize="12" Margin="0,0,0,5"/>
                        <DatePicker SelectedDate="{Binding EndDate}" 
                                    FontSize="14" 
                                    Width="140"/>
                    </StackPanel>

                    <!-- Cashier Filter -->
                    <StackPanel Grid.Column="2" Margin="0,0,15,0">
                        <TextBlock Text="الكاشير" FontSize="12" Margin="0,0,0,5"/>
                        <ComboBox ItemsSource="{Binding Cashiers}" 
                                  SelectedItem="{Binding SelectedCashier}"
                                  DisplayMemberPath="Username"
                                  FontSize="14" 
                                  Width="150">
                            <ComboBox.ItemContainerStyle>
                                <Style TargetType="ComboBoxItem">
                                    <Setter Property="FlowDirection" Value="RightToLeft"/>
                                </Style>
                            </ComboBox.ItemContainerStyle>
                        </ComboBox>
                    </StackPanel>

                    <!-- Payment Method Filter -->
                    <StackPanel Grid.Column="3" Margin="0,0,15,0">
                        <TextBlock Text="طريقة الدفع" FontSize="12" Margin="0,0,0,5"/>
                        <ComboBox SelectedValue="{Binding SelectedPaymentMethod}" 
                                  SelectedValuePath="Tag"
                                  FontSize="14" 
                                  Width="140">
                            <ComboBoxItem Content="الكل" Tag="الكل"/>
                            <ComboBoxItem Content="نقداً" Tag="نقداً"/>
                            <ComboBoxItem Content="بطاقة" Tag="بطاقة"/>
                            <ComboBoxItem Content="تحويل بنكي" Tag="تحويل بنكي"/>
                            <ComboBox.ItemContainerStyle>
                                <Style TargetType="ComboBoxItem">
                                    <Setter Property="FlowDirection" Value="RightToLeft"/>
                                </Style>
                            </ComboBox.ItemContainerStyle>
                        </ComboBox>
                    </StackPanel>

                    <!-- Show Voided -->
                    <StackPanel Grid.Column="4" Margin="0,0,15,0" VerticalAlignment="Bottom">
                        <CheckBox Content="عرض الملغي" 
                                  IsChecked="{Binding ShowVoided}"
                                  FontSize="14"
                                  FlowDirection="RightToLeft"/>
                    </StackPanel>

                    <!-- Search and Reset Buttons -->
                    <StackPanel Grid.Column="5" Margin="0,0,0,0" VerticalAlignment="Bottom" Orientation="Horizontal">
                        <Button Content="🔍 بحث"
                                Command="{Binding SearchCommand}"
                                FontSize="14"
                                FontWeight="Bold"
                                Padding="20,8"
                                Background="#4CAF50"
                                Foreground="White"
                                BorderThickness="0"
                                Cursor="Hand"
                                Margin="0,0,10,0"/>
                        <Button Content="🔄 إلغاء الفلترة"
                                Command="{Binding ResetFiltersCommand}"
                                FontSize="14"
                                FontWeight="Bold"
                                Padding="20,8"
                                Background="#FF9800"
                                Foreground="White"
                                BorderThickness="0"
                                Cursor="Hand"/>
                    </StackPanel>
                </Grid>
            </Grid>
        </Border>

        <!-- Orders List -->
        <Border Grid.Row="1" Background="White" Margin="10,5,10,10">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Orders Count -->
                <TextBlock Grid.Row="0" 
                           Margin="15,15,15,10"
                           FontSize="14" 
                           Foreground="#666666"
                           FlowDirection="RightToLeft">
                    <Run Text="عدد الطلبات: "/>
                    <Run Text="{Binding TotalOrders}" FontWeight="Bold"/>
                    <Run Text=" | الصفحة "/>
                    <Run Text="{Binding CurrentPage}" FontWeight="Bold"/>
                    <Run Text=" من "/>
                    <Run Text="{Binding TotalPages}" FontWeight="Bold"/>
                </TextBlock>

                <!-- Orders List ScrollViewer -->
                <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                    <StackPanel>
                        <!-- Header Row -->
                        <Border Background="#F5F5F5" 
                                BorderBrush="#E0E0E0" 
                                BorderThickness="0,0,0,2" 
                                Padding="10,8">
                            <Grid FlowDirection="RightToLeft">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="110"/>
                                    <ColumnDefinition Width="150"/>
                                    <ColumnDefinition Width="100"/>
                                    <ColumnDefinition Width="100"/>
                                    <ColumnDefinition Width="120"/>
                                    <ColumnDefinition Width="70"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <TextBlock Grid.Column="0" Text="رقم الطلب" FontSize="12" FontWeight="Bold" Foreground="#333"/>
                                <TextBlock Grid.Column="1" Text="التاريخ والوقت" FontSize="12" FontWeight="Bold" Foreground="#333"/>
                                <TextBlock Grid.Column="2" Text="الكاشير" FontSize="12" FontWeight="Bold" Foreground="#333"/>
                                <TextBlock Grid.Column="3" Text="طريقة الدفع" FontSize="12" FontWeight="Bold" Foreground="#333"/>
                                <TextBlock Grid.Column="4" Text="الإجمالي" FontSize="12" FontWeight="Bold" Foreground="#333"/>
                                <TextBlock Grid.Column="5" Text="الحالة" FontSize="12" FontWeight="Bold" Foreground="#333" HorizontalAlignment="Center"/>
                                <TextBlock Grid.Column="6" Text="الإجراءات" FontSize="12" FontWeight="Bold" Foreground="#333"/>
                            </Grid>
                        </Border>

                        <!-- Orders List -->
                        <ItemsControl ItemsSource="{Binding Orders}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Background="White" 
                                    BorderBrush="#E0E0E0" 
                                    BorderThickness="0,0,0,1" 
                                    Padding="10,8" 
                                    Margin="0">
                                <Grid FlowDirection="RightToLeft">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="110"/>
                                        <ColumnDefinition Width="150"/>
                                        <ColumnDefinition Width="100"/>
                                        <ColumnDefinition Width="100"/>
                                        <ColumnDefinition Width="120"/>
                                        <ColumnDefinition Width="70"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>

                                    <!-- Order Number -->
                                    <TextBlock Grid.Column="0" 
                                               FontSize="13" 
                                               FontWeight="Bold" 
                                               Foreground="#2196F3"
                                               VerticalAlignment="Center"
                                               Text="{Binding OrderNumber}"/>

                                    <!-- Date Time -->
                                    <TextBlock Grid.Column="1" 
                                               FontSize="12" 
                                               Foreground="#666666"
                                               VerticalAlignment="Center"
                                               Text="{Binding OrderDateTime, StringFormat='yyyy/MM/dd HH:mm'}"/>

                                    <!-- Cashier -->
                                    <TextBlock Grid.Column="2" 
                                               FontSize="12" 
                                               Foreground="#666666"
                                               VerticalAlignment="Center"
                                               Text="{Binding Cashier.Username}"/>

                                    <!-- Payment Method -->
                                    <TextBlock Grid.Column="3" 
                                               FontSize="12" 
                                               Foreground="#666666"
                                               VerticalAlignment="Center"
                                               Text="{Binding PaymentMethod}"/>

                                    <!-- Total Amount -->
                                    <TextBlock Grid.Column="4" 
                                               FontSize="13" 
                                               FontWeight="Bold" 
                                               Foreground="#4CAF50"
                                               VerticalAlignment="Center">
                                        <Run Text="{Binding TotalAmount, StringFormat='{}{0:N2}'}"/>
                                        <Run Text=" جنيه"/>
                                    </TextBlock>

                                    <!-- Status Badge -->
                                    <Border Grid.Column="5" 
                                            VerticalAlignment="Center"
                                            HorizontalAlignment="Center"
                                            Padding="8,3"
                                            CornerRadius="3">
                                        <Border.Style>
                                            <Style TargetType="Border">
                                                <Setter Property="Background" Value="#4CAF50"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsVoided}" Value="True">
                                                        <Setter Property="Background" Value="#F44336"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Border.Style>
                                        <TextBlock FontSize="10" FontWeight="Bold" Foreground="White">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock">
                                                    <Setter Property="Text" Value="نشط"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding IsVoided}" Value="True">
                                                            <Setter Property="Text" Value="ملغي"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>
                                    </Border>

                                    <!-- Action Buttons -->
                                    <StackPanel Grid.Column="6" 
                                                Orientation="Horizontal" 
                                                HorizontalAlignment="Left"
                                                VerticalAlignment="Center"
                                                Margin="10,0,0,0">
                                        <Button Content="📋"
                                                Command="{Binding DataContext.ViewDetailsCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                                CommandParameter="{Binding}"
                                                FontSize="12"
                                                Padding="8,4"
                                                Background="#2196F3"
                                                Foreground="White"
                                                BorderThickness="0"
                                                Cursor="Hand"
                                                ToolTip="عرض التفاصيل"
                                                Margin="0,0,5,0"/>

                                        <Button Content="❌"
                                                Command="{Binding DataContext.VoidOrderCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                                CommandParameter="{Binding}"
                                                FontSize="12"
                                                Padding="8,4"
                                                Background="#F44336"
                                                Foreground="White"
                                                BorderThickness="0"
                                                Cursor="Hand"
                                                ToolTip="إلغاء الطلب">
                                            <Button.Style>
                                                <Style TargetType="Button">
                                                    <Setter Property="Visibility" Value="Visible"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding IsVoided}" Value="True">
                                                            <Setter Property="Visibility" Value="Collapsed"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding DataContext.IsAdmin, RelativeSource={RelativeSource AncestorType=ItemsControl}}" Value="False">
                                                            <Setter Property="Visibility" Value="Collapsed"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Button.Style>
                                        </Button>
                                    </StackPanel>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
                    </StackPanel>
            </ScrollViewer>

            <!-- Pagination Controls -->
            <Border Grid.Row="2" 
                    Background="#F5F5F5" 
                    BorderBrush="#E0E0E0" 
                    BorderThickness="0,1,0,0"
                    Padding="15">
                <StackPanel Orientation="Horizontal" 
                            HorizontalAlignment="Center"
                            FlowDirection="LeftToRight">
                    
                    <!-- Previous Button -->
                    <Button Content="◄ السابق"
                            Command="{Binding PreviousPageCommand}"
                            FontSize="14"
                            Padding="15,8"
                            Background="#2196F3"
                            Foreground="White"
                            BorderThickness="0"
                            Cursor="Hand"
                            Margin="0,0,10,0">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Opacity" Value="1"/>
                                <Style.Triggers>
                                    <Trigger Property="IsEnabled" Value="False">
                                        <Setter Property="Opacity" Value="0.5"/>
                                        <Setter Property="Cursor" Value="Arrow"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>

                    <!-- Page Numbers -->
                    <TextBlock FontSize="16" 
                               FontWeight="Bold"
                               VerticalAlignment="Center"
                               Margin="10,0"
                               FlowDirection="RightToLeft">
                        <Run Text="صفحة "/>
                        <Run Text="{Binding CurrentPage}"/>
                        <Run Text=" / "/>
                        <Run Text="{Binding TotalPages}"/>
                    </TextBlock>

                    <!-- Next Button -->
                    <Button Content="التالي ►"
                            Command="{Binding NextPageCommand}"
                            FontSize="14"
                            Padding="15,8"
                            Background="#2196F3"
                            Foreground="White"
                            BorderThickness="0"
                            Cursor="Hand"
                            Margin="10,0,0,0">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Opacity" Value="1"/>
                                <Style.Triggers>
                                    <Trigger Property="IsEnabled" Value="False">
                                        <Setter Property="Opacity" Value="0.5"/>
                                        <Setter Property="Cursor" Value="Arrow"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>
                </StackPanel>
            </Border>
            </Grid>
        </Border>
    </Grid>
</UserControl>

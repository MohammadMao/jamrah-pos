<Window x:Class="JamrahPOS.Views.OrderDetailsDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="تفاصيل الطلب" 
        Height="600" 
        Width="500"
        WindowStartupLocation="CenterOwner"
        WindowStyle="SingleBorderWindow"
        ResizeMode="NoResize"
        FlowDirection="RightToLeft"
        Background="#F5F5F5"
        FontFamily="Arial">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Title -->
        <TextBlock Grid.Row="0" 
                   x:Name="OrderNumberText"
                   Text="رقم الطلب: ORD-XXX" 
                   FontSize="24" 
                   FontWeight="Bold" 
                   HorizontalAlignment="Center"
                   Margin="0,0,0,20"/>

        <!-- Order Info -->
        <Border Grid.Row="1" 
                Background="White" 
                Padding="15" 
                CornerRadius="5"
                Margin="0,0,0,15">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Row="0" Grid.Column="0" Text="التاريخ والوقت:" FontSize="14" FontWeight="SemiBold" Margin="0,0,10,8"/>
                <TextBlock Grid.Row="0" Grid.Column="1" x:Name="DateTimeText" Text="" FontSize="14" Margin="0,0,0,8"/>

                <TextBlock Grid.Row="1" Grid.Column="0" Text="الكاشير:" FontSize="14" FontWeight="SemiBold" Margin="0,0,10,8"/>
                <TextBlock Grid.Row="1" Grid.Column="1" x:Name="CashierText" Text="" FontSize="14" Margin="0,0,0,8"/>

                <TextBlock Grid.Row="2" Grid.Column="0" Text="طريقة الدفع:" FontSize="14" FontWeight="SemiBold" Margin="0,0,10,8"/>
                <TextBlock Grid.Row="2" Grid.Column="1" x:Name="PaymentMethodText" Text="" FontSize="14" Margin="0,0,0,8"/>

                <TextBlock Grid.Row="3" Grid.Column="0" Text="الحالة:" FontSize="14" FontWeight="SemiBold" Margin="0,0,10,0"/>
                <TextBlock Grid.Row="3" Grid.Column="1" x:Name="StatusText" Text="نشط" FontSize="14" FontWeight="Bold"/>
            </Grid>
        </Border>

        <!-- Order Items -->
        <Border Grid.Row="2" 
                Background="White" 
                CornerRadius="5"
                Margin="0,0,0,15">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Border Grid.Row="0" Background="#2196F3" Padding="10" CornerRadius="5,5,0,0">
                    <TextBlock Text="الأصناف" FontSize="16" FontWeight="Bold" Foreground="White"/>
                </Border>

                <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                    <ItemsControl x:Name="ItemsListControl" Padding="10">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border BorderBrush="#E0E0E0" BorderThickness="0,0,0,1" Padding="0,10">
                                    <Grid>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="Auto"/>
                                        </Grid.RowDefinitions>

                                        <StackPanel Grid.Row="0" Orientation="Horizontal">
                                            <TextBlock Text="{Binding MenuItem.Name}" FontSize="14" FontWeight="SemiBold"/>
                                            <TextBlock Text=" × " FontSize="14" Margin="5,0"/>
                                            <TextBlock Text="{Binding Quantity}" FontSize="14"/>
                                        </StackPanel>

                                        <Grid Grid.Row="1" Margin="0,5,0,0">
                                            <TextBlock HorizontalAlignment="Left">
                                                <Run Text="{Binding UnitPrice, StringFormat='{}{0:N2}'}" FontSize="13" Foreground="#666666"/>
                                                <Run Text=" SDG" FontSize="13" Foreground="#666666"/>
                                            </TextBlock>
                                            <TextBlock HorizontalAlignment="Right">
                                                <Run Text="المجموع: " FontSize="13" FontWeight="SemiBold" Foreground="#4CAF50"/>
                                                <Run Text="{Binding TotalPrice, StringFormat='{}{0:N2}'}" FontSize="13" FontWeight="Bold" Foreground="#4CAF50"/>
                                                <Run Text=" SDG" FontSize="13" FontWeight="SemiBold" Foreground="#4CAF50"/>
                                            </TextBlock>
                                        </Grid>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
            </Grid>
        </Border>

        <!-- Total -->
        <Border Grid.Row="3" 
                Background="#FFF3E0" 
                Padding="15" 
                CornerRadius="5"
                Margin="0,0,0,15">
            <Grid>
                <TextBlock Text="الإجمالي" FontSize="18" FontWeight="Bold" HorizontalAlignment="Right"/>
                <TextBlock x:Name="TotalAmountText" 
                           Text="0.00 SDG" 
                           FontSize="24" 
                           FontWeight="Bold" 
                           Foreground="#FF6F00" 
                           HorizontalAlignment="Left"/>
            </Grid>
        </Border>

        <!-- Action Buttons -->
        <Grid Grid.Row="4">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="10"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Button Grid.Column="0"
                    Content="🖨 طباعة"
                    FontSize="16"
                    FontWeight="Bold"
                    Padding="0,15"
                    Background="#4CAF50"
                    Foreground="White"
                    BorderThickness="0"
                    Cursor="Hand"
                    Click="Print_Click"/>

            <Button Grid.Column="2"
                    Content="إغلاق"
                    FontSize="16"
                    FontWeight="Bold"
                    Padding="0,15"
                    Background="#2196F3"
                    Foreground="White"
                    BorderThickness="0"
                    Cursor="Hand"
                    Click="Close_Click"/>
        </Grid>
    </Grid>
</Window>

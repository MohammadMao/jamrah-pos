<UserControl x:Class="JamrahPOS.Views.PosView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:viewmodels="clr-namespace:JamrahPOS.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="1200">
    
    <UserControl.DataContext>
        <viewmodels:PosViewModel />
    </UserControl.DataContext>

    <Grid Background="#F5F5F5">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Left Section: Menu Items -->
        <Grid Grid.Column="0" Margin="10">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Category Filter -->
            <Border Grid.Row="0" Background="White" Padding="10" CornerRadius="5" Margin="0,0,0,10">
                <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Disabled">
                    <StackPanel Orientation="Horizontal" FlowDirection="RightToLeft">
                        <Button Content="الكل" Command="{Binding ShowAllCategoriesCommand}" FontSize="14" FontWeight="Bold" Padding="20,10" Margin="5" Background="#2196F3" Foreground="White" BorderThickness="0" Cursor="Hand"/>
                        <ItemsControl ItemsSource="{Binding Categories}">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <StackPanel Orientation="Horizontal"/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Button Content="{Binding Name}" FontSize="14" FontWeight="SemiBold" Padding="20,10" Margin="5" Background="#4CAF50" Foreground="White" BorderThickness="0" Cursor="Hand" Click="CategoryButton_Click"/>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </ScrollViewer>
            </Border>

            <!-- Menu Items Grid -->
            <Border Grid.Row="1" Background="White" CornerRadius="5">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <ItemsControl ItemsSource="{Binding MenuItems}" Padding="10">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <WrapPanel Orientation="Horizontal"/>
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Button Command="{Binding DataContext.AddItemCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}" CommandParameter="{Binding}" Width="150" Height="150" Margin="5" Background="White" BorderBrush="#E0E0E0" BorderThickness="1" Cursor="Hand">
                                    <StackPanel VerticalAlignment="Center">
                                        <TextBlock Text="{Binding Name}" FontSize="16" FontWeight="SemiBold" TextWrapping="Wrap" HorizontalAlignment="Center" TextAlignment="Center" FlowDirection="RightToLeft" Margin="0,0,0,10"/>
                                        <TextBlock Text="{Binding Price, StringFormat='{}{0:N2} ریال'}" FontSize="18" FontWeight="Bold" Foreground="#4CAF50" HorizontalAlignment="Center"/>
                                    </StackPanel>
                                </Button>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
            </Border>
        </Grid>

        <!-- Right Section: Order Details -->
        <Grid Grid.Column="1" Margin="10">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Order Title -->
            <Border Grid.Row="0" Background="#2196F3" Padding="15" CornerRadius="5,5,0,0">
                <TextBlock Text="الطلب الحالي" FontSize="20" FontWeight="Bold" Foreground="White" HorizontalAlignment="Center" FlowDirection="RightToLeft"/>
            </Border>

            <!-- Cart Items List -->
            <Border Grid.Row="1" Background="White" BorderBrush="#E0E0E0" BorderThickness="1,0,1,0">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <ItemsControl ItemsSource="{Binding CartItems}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border BorderBrush="#E0E0E0" BorderThickness="0,0,0,1" Padding="10">
                                    <Grid>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="Auto"/>
                                        </Grid.RowDefinitions>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <StackPanel Grid.Row="0" Grid.Column="0" FlowDirection="RightToLeft">
                                            <TextBlock Text="{Binding Name}" FontSize="14" FontWeight="SemiBold" TextWrapping="Wrap"/>
                                            <StackPanel Orientation="Horizontal" Margin="0,5,0,0">
                                                <TextBlock Text="{Binding Quantity}" FontSize="12" Foreground="#666666"/>
                                                <TextBlock Text=" × " FontSize="12" Foreground="#666666"/>
                                                <TextBlock Text="{Binding UnitPrice, StringFormat='{}{0:N2}'}" FontSize="12" Foreground="#666666"/>
                                                <TextBlock Text=" ريال" FontSize="12" Foreground="#666666"/>
                                            </StackPanel>
                                        </StackPanel>
                                        <Button Grid.Row="0" Grid.Column="1" Command="{Binding DataContext.RemoveItemCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}" CommandParameter="{Binding}" Content="×" Width="30" Height="30" FontSize="20" FontWeight="Bold" Background="#F44336" Foreground="White" BorderThickness="0" Cursor="Hand" VerticalAlignment="Top"/>
                                        <TextBlock Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2" Text="{Binding TotalPrice, StringFormat='المجموع: {0:N2} ریال'}" FontSize="14" FontWeight="Bold" Foreground="#4CAF50" HorizontalAlignment="Right" FlowDirection="RightToLeft" Margin="0,5,0,0"/>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
            </Border>

            <!-- Total Amount -->
            <Border Grid.Row="2" Background="#FFF3E0" Padding="15" BorderBrush="#E0E0E0" BorderThickness="1,0">
                <Grid>
                    <TextBlock Text="الإجمالي" FontSize="18" FontWeight="Bold" HorizontalAlignment="Right" FlowDirection="RightToLeft"/>
                    <TextBlock Text="{Binding TotalAmount, StringFormat='{}{0:N2} ریال'}" FontSize="24" FontWeight="Bold" Foreground="#FF6F00" HorizontalAlignment="Left"/>
                </Grid>
            </Border>

            <!-- Action Buttons -->
            <Grid Grid.Row="3" Margin="0,10,0,0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Button Grid.Row="0" Content="إتمام الطلب" Command="{Binding CheckoutCommand}" FontSize="18" FontWeight="Bold" Padding="0,15" Background="#4CAF50" Foreground="White" BorderThickness="0" Cursor="Hand" Margin="0,0,0,10"/>
                <Button Grid.Row="1" Content="مسح الطلب" Command="{Binding ClearCartCommand}" FontSize="16" Padding="0,12" Background="#F44336" Foreground="White" BorderThickness="0" Cursor="Hand"/>
            </Grid>
        </Grid>
    </Grid>
</UserControl>
